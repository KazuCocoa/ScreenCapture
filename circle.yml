machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-19,build-tools-19.1.0,extra-android-support"

  override:
    - ./gradlew dependencies

test:
  override:
    - ./gradlew assembleDebug